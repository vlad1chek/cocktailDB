import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    paddingTop: 20,
  },
  sectionTitle: {
    marginLeft: 20,
    marginBottom: 20,
    fontSize: 14,
    color: '#7E7E7E',
  },
});

export default styles;
